<%= javascript('pebbles/jquery/form', 'pebbles/jquery/livequery') %>

<%= link_to "Delete the Category '#{@category.name}'", admin_category_path(@category),
      :confirm => "Are you sure you want to delete the category #{@category.name}?", :method => :delete %>
<br/><br/>
<%= render 'form', :category => @category %>
<%= render :partial => "images", :locals => { :category => @category } %>
